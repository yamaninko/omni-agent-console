**/bin/
**/obj/
**/node_modules/
**/dist/
**/.angular/
.env
.DS_Store
*.user
*.suo

# memanto session memory cache — never commit
MEMORY.md

# Claude Code local state (settings.local.json, worktrees) — machine-specific.
# Previously excluded only via .git/info/exclude, which does not survive a clone.
.claude/

# runtime-generated agent workspace output (repo root only — not frontend feature folders)
/workspace/
